
plugins {
  id 'application'
}

version '0.0'

dependencies {
  compile project(':common')
  compile project(':messagebus')
}

jar {
  manifest {
    attributes 'Implementation-Title': 'game-server',
        'Implementation-Version': version,
        'Build-Date': new Date().format('yyyyMMddHHmmss')
  }
}
